spring:
  application:
    name: spring-boot-localstack
  config:
    activate:
      on-profile: localstack

cloud:
  aws:
    sqs:
      endpoint: https://localhost.localstack.cloud:4566
    sns:
      endpoint: https://localhost.localstack.cloud:4566
    region:
      static: us-east-1